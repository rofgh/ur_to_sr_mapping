# output_to_terminal.py
# Functions that produce a terminal output

def final_count(tree_count, outputfoldername):
    print(
        "\nAssessed "
        + str(tree_count)
        + " trees and wrote them to the '"
        + outputfoldername
        + "' series\n"
    )